version: "3.8"

services:
  worker:
    image: deepbounty/worker:dev
    deploy:
      mode: replicated
      # Number of worker
      replicas: 2
    build:
      context: .
      dockerfile: Dockerfile.dev
    # Mount source code for hot reload
    volumes:
      # Source code (for hot reload)
      - ./src:/app/src
      - /app/node_modules
    # Environment variables
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    network_mode: host
